import { h, defineComponent } from 'vue'

export default defineComponent({
    name: 'JsonForm',
    props: {
        options: String,
    },
    setup() {},
    render() {
        return h('div', null, '开发中...')
    }
})